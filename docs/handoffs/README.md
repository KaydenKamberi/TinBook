# Handoffs

Each agent writes `CP<id>.md` here when it finishes a checkpoint, using the template in `AGENTS.md` §7.
Before starting a checkpoint, read the handoffs for every checkpoint listed in its "Depends on."
   Windows smoke test: PASS (Windows, Python 3.11, piper-tts 1.8.0)
